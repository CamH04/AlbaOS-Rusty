[package]
name = "albaos"
version = "0.1.0"
edition = "2018"

# more stuff because disable stack unwindng
[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
bootloader = "0.9"
volatile = "0.2.6" # dont optimise volatile writes
